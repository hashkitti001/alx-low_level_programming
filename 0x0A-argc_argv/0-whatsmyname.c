#include <stdlib.h>
#include <stdio.h>

/**
 * main - writes the character c to stdout
 * @argc: int
 * @argv: Character
 * Return: 0
 */

int main(int argc __attribute__((unused)), char *argv[])
{
		printf("%s\n", argv[0]);

		return (0);
}
