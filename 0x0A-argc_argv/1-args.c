#include <stdio.h>

/**
 * main - writes the character c to stdout
 * @argc: Integer
 * @argv: Character
 * Return: 0
 */

int main(int argc, char __attribute__((unused)) *argv[])
{
			printf("%d\n", (argc - 1));

	return (0);
}
